# react-redux-seed
Seed project for React SPA

Need io.js 2.5

# Installing

`npm install`

# Running locally (development)

`npm start`

# Testing

`npm test`

# Release

`NODE_ENV=PRODUCTION npm run build`
